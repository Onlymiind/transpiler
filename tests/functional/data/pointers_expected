// clang-format off
#include <stdint.h>

typedef uint64_t u64;
typedef uint8_t bool;
typedef double f64;

void foo(u64*** a, f64* b);
void foo(u64*** a, f64* b) {
(*b = (f64)((u64)1000 * ***a));
}
int main(void) {
u64 a = (u64)11;
u64* ap = &a;
u64** app = &ap;
u64* p_default = (u64*)0;
f64 b = (f64)0;
foo(&app, &b);
u64* c = (u64*)0;
if((ap == c)) {}
}
